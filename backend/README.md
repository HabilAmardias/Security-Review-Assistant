# ase-security-review (backend)

Backend for the ASE Security Review Agent. See the repository root `README.md` for full setup and usage.

```bash
uv sync
uv run pytest          # run the test suite
uv run ase-security-review   # start the API on http://127.0.0.1:8000
```
